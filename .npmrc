# Required for Vercel monorepo deployment
# Prevents peer dependency conflicts in workspace packages
strict-peer-dependencies=false
auto-install-peers=true

# GitHub Packages registry for @sheplang and @adapters packages
@sheplang:registry=https://npm.pkg.github.com
@adapters:registry=https://npm.pkg.github.com
