# VS Code Extension Bundled with esbuild
# See: https://code.visualstudio.com/api/working-with-extensions/bundling-extension
#
# Since we bundle ALL dependencies into dist/extension.js,
# we can exclude node_modules and source files.

# Development files
.vscode/**
.vscode-test/**
src/**
.gitignore
.yarnrc
vsc-extension-quickstart.md
vscode.proposed.*.d.ts

# Config files
**/tsconfig.json
**/.eslintrc.json
tsconfig.tsbuildinfo
esbuild.js
webpack.config.js

# Source maps (optional - remove if debugging needed)
**/*.map

# TypeScript source files
**/*.ts
!**/*.d.ts

# Exclude parent sheplang directory
../sheplang/**

# Node modules - EXCLUDED because we bundle with esbuild
node_modules/**

# Old output directory (now using dist/ for everything)
# Language server is now bundled to dist/server.js via esbuild
out/**

# Demo and test artifacts
test-resources/**
demo-workspace/**
scripts/**

# Environment and secrets
.env
.env.*

# Logs
*.log
trace.log

# OS files
.DS_Store
Thumbs.db

# Keep these files:
# - dist/extension.js (bundled code)
# - package.json
# - README.md
# - media/** (icons, images)
# - syntaxes/** (TextMate grammars)
# - snippets/** (code snippets)
# - language-configuration.json
