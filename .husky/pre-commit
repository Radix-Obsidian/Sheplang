#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running ShepLang VERIFY..."
powershell -ExecutionPolicy Bypass -File ./scripts/verify.ps1
RESULT=$?
if [ $RESULT -ne 0 ]; then
  echo "❌ VERIFY failed — commit aborted."
  exit 1
fi

echo "✅ VERIFY passed."
