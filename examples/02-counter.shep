app MyCounter

data Counter:
  fields:
    value: number
    label: text
  rules:
    - "value must be a number"

view Dashboard:
  list Counter
  button "Add Counter" -> AddCounter

action AddCounter(label):
  add Counter with label, value=1
  show Dashboard
